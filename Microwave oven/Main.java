#include<iostream>
using namespace std;
int main()
{
  //Type your code here.
    int a,b;
  cin>>a>>b;
  if(a>3)
    cout<<"Number of items is more";
  else if(a==2)
    cout<<b+b*0.5;
  else if(a==3)
    cout<<b+b;
}